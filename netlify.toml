# Netlify configuration (issue #62; also #180).
#
# The Astro project lives at the repository root. Connect this repository as a
# Netlify site to get automatic Deploy Previews for pull requests.
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
